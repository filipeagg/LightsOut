# Example agent profiles and policy packs

Phase 3 populates this directory with sample agent profiles (`*.yaml`) and policy
packs (`policies/*.yaml`). On first boot the contents are copied into
`$LIGHTSOUT_WORKSPACE/agents/` when that directory is empty (DESIGN §11.1 step 4).
