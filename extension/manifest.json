{
  "manifest_version": "0.3",
  "name": "lightsout",
  "display_name": "LightsOut",
  "version": "0.2.0",
  "description": "Run chains of coding agents unattended, with mediated permissions and doubts you answer.",
  "long_description": "LightsOut orchestrates coding agents (Claude Code and Codex) inside a container on your machine. Launch a chain of tasks and it runs them unattended: it commits per task, runs your verify command, and stops to ask only when a decision is genuinely yours. It also keeps curated knowledge bases you can search and read from any conversation. This extension is a thin local bridge to the container's MCP endpoint at 127.0.0.1; nothing leaves your machine.",
  "author": {
    "name": "LightsOut"
  },
  "license": "Apache-2.0",
  "keywords": ["agents", "orchestration", "docker", "development"],
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/index.js"],
      "env": {
        "LO_MCP_URL": "http://127.0.0.1:${user_config.port}/mcp"
      }
    }
  },
  "user_config": {
    "port": {
      "type": "number",
      "title": "LightsOut port",
      "description": "The port the LightsOut container publishes on this machine. Leave 8484 unless you changed it.",
      "default": 8484,
      "min": 1,
      "max": 65535,
      "required": false
    }
  },
  "tools_generated": true,
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": ["win32", "darwin", "linux"],
    "runtimes": {
      "node": ">=18.0.0"
    }
  }
}
