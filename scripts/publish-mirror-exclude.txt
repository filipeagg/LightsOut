# Paths stripped from every commit before the public mirror is pushed (scripts/publish-mirror.sh).
# One path per line, same syntax as `git filter-repo --path`. Comments start with #.

# Continuity docs: name a real customer host and narrate real client work.
doc/STATE.md
doc/DECISIONS.md

# The redaction list itself: it is a list of the real names, which is exactly what must not be
# published. The mechanism that uses it stays public in publish-mirror.sh; the names do not.
scripts/publish-mirror-redact.txt

# Meta-instructions for this exact Claude Desktop setup, not needed to run LightsOut.
doc/PROJECT-INSTRUCTIONS.md
doc/tester-project-instructions.md

# One-off debugging/probe scripts tied to specific past incidents, superseded by the
# official gates in scripts/verify/*.sh. Not part of the reusable product.
scripts/check-panel.mjs
scripts/inspect-config-audit.mjs
scripts/inspect-loss.mjs
scripts/inspect-run.mjs
scripts/inspect-writes.mjs
scripts/inspect-project-refs.mjs
scripts/mcp-call.mjs
scripts/migrate-packs-pe14.mjs
scripts/probe-classify.mjs
scripts/probe-credgates.mjs
scripts/probe-events.mjs
scripts/probe-summary.mjs
scripts/probe-xlsx.py
scripts/probe-mapa-gates.mjs
scripts/probe-hosts.mjs
scripts/status.mjs
scripts/time-folder-walk.mjs
scripts/verify-2607.mjs
scripts/verify-areas.mjs
scripts/verify-classify-2607.mjs
scripts/verify-deploy.mjs
scripts/verify-docs-context.mjs
scripts/verify-judge.mjs
scripts/verify-migration3.mjs
scripts/verify-pe13.mjs
scripts/verify-pe15.mjs
scripts/verify-resume.mjs
scripts/verify-dotenv-7-1f.mjs

# Belt and braces: this list was hand-maintained and two probe scripts reached the public repo
# because nobody remembered to add them. Every one-off probe/inspect script matches one of these,
# so a new one is stripped whether or not it is named above. The phase gates live in
# scripts/verify/*.sh, a directory, and are untouched by these patterns.
glob:scripts/probe-*
glob:scripts/inspect-*
glob:scripts/verify-*.mjs

# The Windows wrappers for those same one-off scripts: each one hard-codes an absolute path on the
# machine it was written on, and some of them call a script that no longer exists in the mirror.
# The numbered entry points (1-Start-LightsOut.bat and friends) are the product and stay.
glob:scripts/windows/run-*.cmd
glob:scripts/windows/deploy-*.cmd
